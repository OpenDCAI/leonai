{
  "name": "EnterWorktree",
  "description": "Use this tool ONLY when the user explicitly asks to work in a worktree. Creates an isolated git worktree and switches the current session into it. Must be in a git repository or have WorktreeCreate/WorktreeRemove hooks configured.",
  "parameters": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
      "name": {
        "description": "Optional name for the worktree. A random name is generated if not provided.",
        "type": "string"
      }
    },
    "type": "object"
  }
}
