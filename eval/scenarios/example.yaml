id: file_ops_basic
name: "基础文件操作"
category: file_ops
timeout_seconds: 120
sandbox: local
messages:
  - content: "在 /tmp/eval_test/ 创建一个 hello.py，包含一个 hello() 函数，返回 'Hello, World!'"
  - content: "读取 hello.py 并给 hello 函数添加 docstring"
expected_behaviors:
  - "调用 write_file 或 create_file"
  - "调用 read_file"
  - "调用 edit_file"
evaluation_criteria:
  - "任务完成度：是否完成所有请求的操作？"
  - "效率：是否避免了不必要的 tool call？"
